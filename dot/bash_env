if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

PS1='\w \$ '

export EDITOR=vim

# Variables for building in $HOME.
export C_INCLUDE_PATH=$HOME/lib
export LD_LIBRARY_PATH=$HOME/lib
export PKG_CONFIG_PATH=$HOME/lib/pkgconfig
#export MANPATH=$HOME/share/man:$(manpath)
